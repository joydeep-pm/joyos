# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | scope | Product sequencing | Use four milestones: decision/review operating system, people management intelligence, approval-governed automation, expanded orchestration | Matches the current repo state and sequences the next value-bearing capabilities without shrinking the full vision | Yes — if implementation reality or user priorities change |
| D002 | M001 | scope | M001 focus | Prioritize decision and review workflows over people-management or broader automation | Existing V1 already covers sync, intervention, and artifact drafting; the next value gap is operational decision-making before grooming | Yes — if the user explicitly re-prioritizes |
| D003 | M001 | arch | Product framing | Keep the system anchored on the feature-request-centric Product Control Tower model | This preserves the accepted product direction and avoids regression to a generic task planner | No |
| D004 | M001 | convention | Systems-of-record boundary | Continue treating Jira as execution truth, Confluence as documentation truth, and Personal OS as private orchestration overlay | This matches the user’s explicit trust model and keeps writeback safely approval-gated | No |
| D005 | M001 | pattern | M001 planning pattern | Treat prior V1 capabilities as established and plan M001 as the next milestone rather than re-planning already built work | The repo already contains implemented control-tower foundations, so the roadmap should advance from current reality | No |
| D006 | M001/S01 | verification | S01 proof strategy | Prove S01 through contract and integration tests anchored on a new readiness evaluator plus typecheck, without claiming browser/UAT completion in this slice | The slice owns the readiness model and grooming wiring, but durable decision storage and full operating-loop validation belong to later slices | Yes |
| D007 | M001/S01 | pattern | Readiness modeling seam | Introduce grooming readiness as a derived evaluator module consumed by the grooming engine and route, instead of expanding coarse bucket logic directly inside `grooming-engine.ts` | This creates a stable machine-readable contract that downstream slices can reuse for decision tracking and follow-up flows | No |
| D008 | M001/S01 | ui-boundary | Grooming page import boundary | Keep `app/grooming/page.tsx` importing only browser-safe control-tower modules (`types` and `grooming-engine`) instead of the barrel export | The barrel also re-exports server-only cache and ingestion modules, which pull `fs` into the client bundle and break the real grooming page | No |

| D009 | M001/S02 | verification | S02 proof strategy | Prove S02 through review-store and assembler contract tests plus downstream intervention/artifact integration tests and typecheck, without claiming runtime or UAT completion in this slice | The slice owns durable review persistence and cross-surface wiring, but live review capture and end-to-end operating-loop execution remain for S03 | Yes |
| D010 | M001/S02 | arch | Review persistence seam | Store director review metadata in a dedicated local review overlay and assemble it server-side with readiness/intervention data instead of mutating cached feature requests or relying on client-only state | This preserves the systems-of-record boundary, keeps downstream consumers on one enriched contract, and removes the unsafe raw-cache cast from artifact generation | No |
| D011 | M001/S02 | testing | Review store isolation | Resolve review-store paths from `ASSISTANT_CACHE_DIR` at call time and isolate review-store tests in a dedicated cache root | This keeps persisted review fixtures deterministic under Vitest and prevents cross-test pollution from prior local runs while preserving the production cache layout | Yes |
| D012 | M001/S02 | observability | Review-aware API failure contract | Return stable `code` fields plus assembler diagnostics from intervention and artifact routes when review-aware lookup fails | This replaces unsafe raw-cache ambiguity with inspectable API failure payloads and keeps future debugging anchored on the shared assembler output | No |
| D013 | M001/S03 | verification | S03 proof strategy | Prove S03 with failing-first route and handoff tests, typecheck, and a real browser pass through review capture → artifact generation → approval-gated draft creation | S03 owns final assembly of the pre-grooming loop, so code-level contracts alone are insufficient without live runtime proof | No |
| D014 | M001/S03 | integration | Review capture seam | Add review creation/editing through a dedicated control-tower review mutation API and refresh runtime state from assembled feature-request reads instead of client-local patching or raw-cache access | This preserves the overlay model, keeps the UI on the shared assembled contract, and avoids duplicating workflow state | No |
| D015 | M001/S03 | approval | Artifact handoff fidelity | Artifact submission to comms must persist the generated subject/body/destination as a draft while preserving the separate approve/send steps | The slice only proves the operating loop if follow-up drafting survives handoff intact without bypassing the user approval boundary | No |
| D016 | M001/S03 | ui | Follow-up action hooks | Intervention detail follow-up and clarification callbacks should be side-effect free unless they add durable workflow behavior beyond artifact generation | Placeholder alerts obscured the real artifact-generation loop and added noise without contributing inspectable runtime state | Yes |
